# Documentation toolchain for the PatrolBot docs site.
# Install with: pip install -r requirements.txt
# Serve locally with: mkdocs serve
# Build static site with: mkdocs build

mkdocs-material>=9.5
# fence_div_format renders Mermaid as <div> blocks; mermaid-config.js handles
# rendering, theme adaptation, instant-navigation re-render, and zoom/lightbox.
